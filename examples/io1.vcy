int main(int argc, string[] argv) {
	
	string fname1 = "test.vcy";
	string fname2 = "out.txt";
	in_channel i = open_read(fname);
	in_channel j = open_read(fname);
	string data1 = "deadbeef";
	string data2 = "helloworld";
	int y = 2;
	commute _ {
		{ i = open_read(fname1); 
          data1 = read_line(i); 
          /* Process data1 */
          close(i); }
		{ j = open_read(fname2); 
		  data2 = read_line(j);
		  /* Process data2 */
		  close(j); }
	}
	return 0;
}
